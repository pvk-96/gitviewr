{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the main GitViewr window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "dialog:default"
  ]
}